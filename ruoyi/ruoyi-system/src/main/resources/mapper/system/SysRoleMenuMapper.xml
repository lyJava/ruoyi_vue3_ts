<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysRoleMenuMapper">

	<resultMap type="SysRoleMenu" id="SysRoleMenuResult">
		<result property="roleId" jdbcType="BIGINT" column="role_id"/>
		<result property="menuId" jdbcType="BIGINT" column="menu_id"/>
		<result property="createTime" jdbcType="TIMESTAMP" column="create_time"/>
		<result property="updateTime" jdbcType="TIMESTAMP" column="update_time"/>
	</resultMap>
	
	<select id="checkMenuExistRole" resultType="java.lang.Integer">
	    SELECT COUNT(*) FROM sys_role_menu WHERE menu_id = #{menuId,jdbcType=BIGINT}
	</select>

	<delete id="deleteRoleMenuByRoleId" parameterType="Long">
		DELETE FROM sys_role_menu WHERE role_id = #{roleId,jdbcType=BIGINT}
	</delete>
	
	<delete id="deleteRoleMenu" parameterType="Long">
 		DELETE FROM sys_role_menu WHERE role_id IN
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId,jdbcType=BIGINT}
        </foreach> 
 	</delete>
	
	<insert id="batchRoleMenu">
		INSERT INTO sys_role_menu(role_id, menu_id, create_time, update_time) VALUES
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.roleId,jdbcType=BIGINT}, #{item.menuId,jdbcType=BIGINT}, NOW(), NOW())
		</foreach>
	</insert>
	
</mapper> 