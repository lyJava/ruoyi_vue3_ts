<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.quartz.mapper.SysJobLogMapper">
    <resultMap type="SysJobLog" id="SysJobLogResult">
        <id property="jobLogId" jdbcType="BIGINT" column="job_log_id"/>
        <result property="jobName" jdbcType="VARCHAR" column="job_name"/>
        <result property="jobGroup" jdbcType="VARCHAR" column="job_group"/>
        <result property="invokeTarget" jdbcType="VARCHAR" column="invoke_target"/>
        <result property="jobMessage" jdbcType="VARCHAR" column="job_message"/>
        <result property="status" jdbcType="VARCHAR" column="status"/>
        <result property="exceptionInfo" jdbcType="LONGVARCHAR" column="exception_info"/>
        <result property="createTime" jdbcType="TIMESTAMP" column="create_time"/>
    </resultMap>

    <sql id="selectJobLogVo">
         SELECT job_log_id, job_name, job_group, invoke_target, job_message, status, exception_info, create_time FROM sys_job_log
    </sql>

    <select id="selectJobLogList" parameterType="SysJobLog" resultMap="SysJobLogResult">
        <include refid="selectJobLogVo"/>
        <where>
            <if test="jobName != null and jobName != ''">
                job_name LIKE CONCAT('%', #{jobName,jdbcType=VARCHAR}, '%')
            </if>
            <if test="jobGroup != null and jobGroup != ''">
                AND job_group = #{jobGroup,jdbcType=VARCHAR}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status,jdbcType=VARCHAR}
            </if>
            <if test="invokeTarget != null and invokeTarget != ''">
                AND invoke_target LIKE CONCAT('%', #{invokeTarget,jdbcType=VARCHAR}, '%')
            </if>
            <if test="params.beginTime != null and params.beginTime != ''">
                <!-- 开始时间检索 -->
                AND date_format(create_time, '%y%m%d') &gt;= date_format(#{params.beginTime,jdbcType=VARCHAR}, '%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                <!-- 结束时间检索 -->
                AND date_format(create_time, '%y%m%d') &lt;= date_format(#{params.endTime,jdbcType=VARCHAR}, '%y%m%d')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectJobLogAll" resultMap="SysJobLogResult">
        <include refid="selectJobLogVo"/>
    </select>

    <select id="selectJobLogById" parameterType="Long" resultMap="SysJobLogResult">
        <include refid="selectJobLogVo"/>
        WHERE job_log_id = #{jobLogId}
    </select>

    <delete id="deleteJobLogById" parameterType="Long">
        DELETE
        FROM sys_job_log
        WHERE job_log_id = #{jobLogId,jdbcType=BIGINT}
    </delete>

    <delete id="deleteJobLogByIds" parameterType="Long">
        DELETE
        FROM sys_job_log WHERE job_log_id IN
        <foreach collection="array" item="jobLogId" open="(" separator="," close=")">
            #{jobLogId,jdbcType=BIGINT}
        </foreach>
    </delete>

    <update id="cleanJobLog">
        TRUNCATE TABLE sys_job_log
    </update>

    <insert id="insertJobLog" parameterType="SysJobLog">
        INSERT INTO sys_job_log(
        <if test="jobLogId != null and jobLogId != 0">
            job_log_id,
        </if>
        <if test="jobName != null and jobName != ''">
            job_name,
        </if>
        <if test="jobGroup != null and jobGroup != ''">
            job_group,
        </if>
        <if test="invokeTarget != null and invokeTarget != ''">
            invoke_target,
        </if>
        <if test="jobMessage != null and jobMessage != ''">
            job_message,
        </if>
        <if test="status != null and status != ''">
            status,
        </if>
        <if test="exceptionInfo != null and exceptionInfo != ''">
            exception_info,
        </if>
        create_time
                )values(
        <if test="jobLogId != null and jobLogId != 0">
            #{jobLogId,jdbcType=BIGINT},
        </if>
        <if test="jobName != null and jobName != ''">
            #{jobName,jdbcType=VARCHAR},
        </if>
        <if test="jobGroup != null and jobGroup != ''">
            #{jobGroup,jdbcType=VARCHAR},
        </if>
        <if test="invokeTarget != null and invokeTarget != ''">
            #{invokeTarget,jdbcType=VARCHAR},
        </if>
        <if test="jobMessage != null and jobMessage != ''">
            #{jobMessage,jdbcType=VARCHAR},
        </if>
        <if test="status != null and status != ''">
            #{status,jdbcType=VARCHAR},
        </if>
        <if test="exceptionInfo != null and exceptionInfo != ''">
            #{exceptionInfo,jdbcType=LONGVARCHAR},
        </if>
        NOW()
                )
    </insert>
</mapper>